# Atrium Details (static)

Hosted on GitHub Pages. After pushing, enable **Settings → Pages → Source: Deploy from branch** → branch `main`, folder `/ (root)`.

URL will be: `https://<your-username>.github.io/<repo-name>/`
